# V6.2.2 — Structure Rendering Fix

- Removes all top-level `rdkit.Chem.Draw` imports.
- Uses `rdMolDraw2D` directly and lazily when available.
- Adds a pure-Pillow molecular graph renderer as a real structure fallback.
- The fallback draws atoms, aromatic/single/double/triple bonds and highlighted atoms rather than showing a text-only placeholder.
- Keeps PDF display Chrome-safe by using PNG through `st.image()`.
- Pins Python 3.12 through `.python-version` and `runtime.txt`.
- Keeps V6.2.1 route recognition, molecular graph validation, mechanism analysis and reports.
